{
  "name": "durak-online",
  "version": "1.0.0",
  "private": true,
  "description": "Durak, online.",
  "type": "module",
  "scripts": {
    "build": "python3 build.py",
    "dev": "python3 build.py --serve",
    "watch": "python3 build.py --watch",
    "test": "node tests/engine.test.mjs && node tests/sync.test.mjs && node --test tests/rating.test.mjs tests/markup.test.mjs tests/view.test.mjs",
    "test:sql": "bash tests/sql/run.sh"
  },
  "devDependencies": {
    "jsdom": "^30.1.0"
  }
}
